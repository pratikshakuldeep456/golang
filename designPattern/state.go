package designpattern

// behavior changes based on its current internal state,
//  and instead of writing giant if/else or switch blocks checking
//  "what state am I in right now," each state is its own separate
//  object that knows what's
//   allowed to happen in that state and what the next state should be.
